esphome:
  name: m5papermono-lite-test
  friendly_name: M5PaperMono Lite Test

packages:
  m5papermono_lite:
    url: https://github.com/CitizenRacer/M5PaperMonoLite
    ref: main
    file: packages/m5papermono-lite.yaml
    refresh: 1d

logger:

# The hardware package defines the display, touchscreen, frontlight and battery
# telemetry. Add an application display lambda/pages, Wi-Fi, API and OTA in the
# consuming device configuration after the hardware bring-up has been verified.
#
# wifi:
#   ssid: !secret wifi_ssid
#   password: !secret wifi_password
#
# api:
# ota:
#   - platform: esphome
